package observer;
// Observer.java
// Observer interface for Observer pattern
// Defines the update method to be called when the subject changes

public interface Observer {
    void update(String message);
} 